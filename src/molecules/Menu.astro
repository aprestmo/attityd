---
const pathname = new URL(Astro.request.url).pathname;
const currentPath = pathname.slice(1);
---

<!-- <span aria-current={pathname === currentPath}>{pathname}</span> -->

<nav>
  <ul>
    <li>
      <a aria-current={pathname === currentPath} href="/">Home</a>
    </li>
    <li>
      <a aria-current={pathname === currentPath} href="/work">Work</a>
    </li>
    <li>
      <a aria-current={pathname === currentPath} href="/contact">Contact</a>
    </li>
  </ul>
</nav>

<style>
  ul {
    column-gap: 2em;
    display: flex;
    list-style-type: none;
    padding-inline-start: 0;
  }
  a {
    font-weight: 400;
    font-size: 1.5em;
  }

  a[aria-current='page'] {
    background-color: var(--theme-bg-accent);
    color: var(--theme-text-accent);
    font-weight: 600;
  }
</style>
